# Preservation recovery ledger

This ledger records the recovery review performed from the preserved dirty
checkout. Recovery work is performed only in `codex/preservation-recovery-20260725`.

| Slice | Source reviewed | Disposition | Evidence / next action |
| --- | --- | --- | --- |
| Country-aware gallery and offer discovery | `625181e`, `20f6a02`, related source, migrations, scripts, and tests | Recovered in part; remaining source rejected | Commit `3a231bb` recovers the explicit US/GB Gemsloot routes, canonical metadata, country filtering, and sitemap coverage. Current shared importers already preserve country-scoped stable external IDs. The archived cookie selector, profile-country behavior, and bulk refresher are rejected: they add broader behavior, bulk-load complete environment files, and would run remote imports. |
| Public SEO and discovery | `6fc9f7e`, `52a4186`, `330915c`, 37 independent paths | Pending hunk-level review | Current remote history includes a later redesign/revert sequence; do not replay archived visual or SEO patches wholesale. |
| Homepage feature work | `8e931c5` | Already present / superseded | Current `master` includes the remote homepage-feature lineage. Archived migration timestamps must not be replayed. |
| Account foundation and partner tracking | `eace214`, `66a7945` | Superseded / recovered remotely | Current `master` contains the merged account-partner slice. The two old-timestamp migration files are line-equivalent to current migrations and are excluded. |
| PayPal blog migration | `supabase/migrations/20260629093000_upgrade_paypal_rewards_blog_post.sql` | Rejected: unverified content overwrite | Current migration `20260621000500_seed_ten_more_seo_blog_posts.sql` already seeds the same slug. The preserved UPDATE changes editorial copy only and has no corresponding rendered-content or database evidence; do not replay a stale data overwrite. |
| Guide generator | `scripts/generate-live-seo-pipeline-guides.mjs` | Rejected: stale, production-fetching draft generator | It hard-codes `2026-06-29`, fetches live production routes, and emits artifacts marked publish-QA BLOCKED. Current repository workflows must remain source-backed and rendered-route validated. |
| Generated guides | 482 files under `guides/` | Rejected: evidence-only generated output | 42 guide artifacts are already tracked, but the preserved 482 are untracked drafts from the rejected generator and include duplicate slug variants. Never bulk-stage them; future guides require a current source and rendered QA. |
| Codex files and logs | `.codex/*` | Excluded local-only | Never include in a source commit. |
| Package, lockfile, and configuration variants | preserved `package.json`, `package-lock.json`, `.gitignore`, `.codex/*`, `AGENTS.md` | Rejected / local-only | The archived package version removes current release checks, changes Supabase without validation evidence, and only relocates `pg`. `.codex` files are local configuration/logs; preserve neither in source history. The authoritative `AGENTS.md` and package worktree changes stay user-owned evidence, not recovery input. |
| Homepage migration timestamp variants | `20260713150000/153000/154000` versus current timestamped homepage-featured migrations | Duplicate / superseded | The same homepage feature was already recovered remotely; replaying alternative migration timestamps risks duplicate schema operations. |
| Archived release and account documentation | `docs/account-*`, `docs/production-migration-reconciliation.md` | Retained in current master / archive-only history | Current clean lineage retains its release documentation. Do not replay archived deletions or duplicate old operational records. || Remote-only physical gaps | 70 paths present in `master` but absent from preserved checkout | Excluded | The preserved checkout is not a build target; never stage those absences as deletions. |
