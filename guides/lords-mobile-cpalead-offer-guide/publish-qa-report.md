# Publish QA Report

Result: BLOCKED

Publish QA is blocked because no rendered route, deployment URL, or local app target was provided. Content QA passed, but rendered checks such as route status, canonical, H1, metadata, sitemap inclusion, mobile overflow, and content parity cannot be approved without a page to inspect.
