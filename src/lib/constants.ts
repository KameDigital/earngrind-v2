/**
 * Site-wide constants.
 * ─────────────────────────────────────────────
 * Update values here — they are imported across
 * multiple pages so you only need to change one place.
 */

/** Contact email shown on all /legal pages. Replace with your real address before launch. */
export const LEGAL_EMAIL = "legal@earngrind.com";
